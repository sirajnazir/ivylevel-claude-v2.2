# IvyQuest v10.0 Agent Service Dependencies
# =====================================================
# 3P Stack: Agno + LangGraph + AutoGen
# =====================================================

# Core Agent Framework
agno>=0.1.0

# LangChain/LangGraph for deliberation graphs
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# AutoGen for offline debates (selective use)
pyautogen>=0.2.0

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database
supabase>=2.10.0
asyncpg>=0.30.0

# HTTP client
httpx>=0.28.0

# OpenAI for LLM calls
openai>=1.55.0

# Environment management
python-dotenv>=1.0.0

# Async support
anyio>=4.7.0

# Scheduling
apscheduler>=3.10.0  # Cron-based workflow scheduling

# Utilities
tenacity>=9.0.0  # Retry logic
structlog>=24.4.0  # Structured logging

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
pytest-mock>=3.11.0

# Type checking
mypy>=1.13.0
types-requests>=2.32.0

# =====================================================
# v13.2 Memory System Dependencies
# =====================================================

# Redis (short-term memory)
redis>=5.0.0
aioredis>=2.0.0

# Vector operations (for embeddings)
numpy>=1.24.0

# Anthropic Claude
anthropic>=0.18.0

# =====================================================
# v5.4 True Autonomous Agents - Critical 15 Patterns
# =====================================================

# Guardrails AI - Safety guardrails for minors (Pattern E6)
guardrails-ai>=0.5.0

# Langfuse - Observability and metrics (Pattern J2)
langfuse>=2.0.0

# Note: tenacity (Pattern H1) and redis (Pattern B1) already included above

# =====================================================
# v9.0 Phase 3 - Final 20 Patterns
# =====================================================

# Token counting for cost tracking and context compression (K4, J5)
tiktoken>=0.5.0

# PII Detection - Microsoft Presidio (F6)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# =====================================================
# Autonomous Intelligence Layer - Asset Loader
# =====================================================

# YAML parsing for coaching asset files
pyyaml>=6.0.0

# JSON Schema validation for asset files
jsonschema>=4.20.0

# =====================================================
# Letta Multi-Agent Integration (Phase 1)
# =====================================================

# Letta client for persistent memory agents
letta>=0.5.0
